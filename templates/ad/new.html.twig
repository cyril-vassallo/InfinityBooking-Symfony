
{% extends 'base.html.twig' %}

{% block title %}
  Creation d'une annonce
{% endblock %}

{% block body %}
    <div class="container">
        <h1>Créer une nouvelle annonce</h1>
        {# {{ form_start(form)}} 
        {{ form_widget(form)}}
        <input type="button" class="btn btn-primary" value="creaer la nouvelle annonce">
        {{ form_end(form)}} #}

        {{form(form)}}
    </div>
{% endblock %}